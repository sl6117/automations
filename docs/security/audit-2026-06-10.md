# Security audit — 2026-06-10 session

Audit of everything downloaded while setting up the foundation. Triggered by concern over
npm supply-chain attacks (e.g. the Sept 2025 "Shai-Hulud" worm and the chalk/debug
maintainer-token compromise).

## Items downloaded this session

| Item | Version | Source | Verdict |
|---|---|---|---|
| Node | 22.22.3 | nodejs.org official tarball | OK — SHA-256 checksum verified during install ("Checksums matched!") |
| direnv | 2.37.1 | Homebrew bottle (official formula) | OK — Homebrew verifies bottle checksums |
| openclaw (global) | 2026.6.5 | npm (285 transitive deps) | OK with note — see below |

## openclaw audit detail

- Maintainer: `steipete` (Peter Steinberger) — matches the known project author.
- Repo: `github.com/openclaw/openclaw`. Integrity hash present (sha512).
- Published 2026-06-09 (~1 day before install). Recent, but normal for this fast-releasing
  project (calver). Noted as a freshness flag, not a red flag.
- `npm config get ignore-scripts` = `false`, so dependency install/postinstall scripts were
  permitted to run during the global install (standard npm behavior).
- Full-tree `npm audit` (resolved with `--ignore-scripts`, nothing executed):
  - **No malware advisories.**
  - **No critical or high vulnerabilities.**
  - 1 moderate: `hono <=4.12.20` (transitive) — IPv6 deny-rule bypass, cookie/JWT edge cases.
    Low practical risk because the OpenClaw gateway binds to loopback. Revisit with
    `npm audit fix` when upstream bumps `hono`.

## Conclusion
Safe to proceed. Re-run the audit after major OpenClaw upgrades.
